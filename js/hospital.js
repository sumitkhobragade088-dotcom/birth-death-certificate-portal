const form=document.getElementById('hospitalForm'),list=document.getElementById('hospitalList');
function render(){const rows=JSON.parse(localStorage.getItem('birthHospitals')||'[]');list.innerHTML=rows.length?rows.map((r,i)=>`<div class="panel"><b>${r.nameEn}</b><br>${r.nameLocal||''}<br>${r.type} — ${r.taluka}, ${r.district}, ${r.state}<br><small>${r.designation}</small></div>`).join(''):'No saved hospitals in this local demo yet.'}
form.addEventListener('submit',e=>{e.preventDefault();const r={state:state.value,district:district.value,taluka:taluka.value,type:type.value,nameEn:nameEn.value,nameLocal:nameLocal.value,department:department.value,designation:designation.value};const a=JSON.parse(localStorage.getItem('birthHospitals')||'[]');a.push(r);localStorage.setItem('birthHospitals',JSON.stringify(a));form.reset();render();});render();